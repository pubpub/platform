diff --git a/router.esm.js b/router.esm.js
index 75f2cb14bbfaee60524bf28a9f5cef3a90612a71..9e198ec9caecd8654ace5656246f87aff8d3d8d3 100644
--- a/router.esm.js
+++ b/router.esm.js
@@ -139,9 +139,9 @@ const tsRestMiddleware = (options) => {
     const basePath = (_a = options.basePath) !== null && _a !== void 0 ? _a : '';
     const basePathChecker = (request) => {
         const pathname = new URL(request.url).pathname;
-        if (!pathname.startsWith(basePath)) {
-            throw new Error(`Expected path to start with the basePath of ${basePath}, but got a path of ${pathname}`);
-        }
+        // if (!pathname.startsWith(basePath)) {
+        //     throw new Error(`Expected path to start with the basePath of ${basePath}, but got a path of ${pathname}`);
+        // }
     };
     const preflight = (request) => {
         const preflightResult = ittyPreflight(request);
