apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: pubstar-ingress
  labels:
    app.kubernetes.io/name: pubstar
  annotations:
    traefik.ingress.kubernetes.io/router.entrypoints: websecure
spec:
  rules:
    - host: pr-__PR_NUMBER__.k3s.pubstar.org
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: caddy
                port:
                  number: 80
    - host: pr-__PR_NUMBER__-assets.k3s.pubstar.org
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: caddy
                port:
                  number: 80
