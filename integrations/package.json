{
	"name": "v7-integrations",
	"private": true,
	"version": "1.0.0",
	"description": "",
	"main": "index.js",
	"scripts": {
		"build": "pnpm run -r build",
		"publish": "pnpm publish -r",
		"ci:version": "pnpm changelog && pnpx changeset version && pnpm install --no-frozen-lockfile && git add .",
		"ci:publish": "pnpm build && git status && pnpm publish -r && pnpm changeset tag"
	},
	"keywords": [],
	"author": "Knowledge Futures, Inc <hello@knowledgefutures.org>",
	"license": "GPL-2.0+",
	"dependencies": {
		"@changesets/cli": "^2.26.2"
	}
}
