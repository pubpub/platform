{
	"name": "v7-integrations",
	"private": true,
	"version": "1.0.0",
	"description": "",
	"main": "index.js",
	"scripts": {
		"build": "pnpm run -r build",
		"publish": "pnpm publish -r",
		"ci:version": "pnpx changeset version && pnpm install && git add .",
		"ci:publish": "pnpm publish -r && pnpm changeset tag"
	},
	"keywords": [],
	"author": "Knowledge Futures, Inc <hello@knowledgefutures.org>",
	"license": "GPL-2.0+",
	"dependencies": {
		"@changesets/cli": "^2.26.2"
	}
}
